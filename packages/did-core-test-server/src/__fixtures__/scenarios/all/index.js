const scenarios = [require('../resolve')];
const request = { scenarios };
// const request = require("./request.json");
const response = require('./response.json');
module.exports = { request, response };
